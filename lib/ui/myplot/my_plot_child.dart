import 'package:flutter/material.dart';

class HomeMyPlotChild extends StatelessWidget {
  HomeMyPlotChild();

  @override
  Widget build(BuildContext context) {
    return SizedBox.expand(
      child: Text("Mocked text")
    );
  }
}

